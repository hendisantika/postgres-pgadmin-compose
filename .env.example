POSTGRES_USER=your_username
POSTGRES_PASSWORD=your_password
POSTGRES_DB=postgres

PGADMIN_DEFAULT_EMAIL=your_email@example.com
PGADMIN_DEFAULT_PASSWORD=your_pgadmin_password

# Docker Nginx ports (for make up-nginx / up-nginx-ssl)
NGINX_HTTP_PORT=80
NGINX_HTTPS_PORT=443
NGINX_POSTGRES_PORT=5433

# Standalone ports (for make up-standalone with host nginx)
# STANDALONE_PGADMIN_PORT=5051
# STANDALONE_MONITOR_PORT=8889
# STANDALONE_POSTGRES_PORT=5432
